# FastAPI and server
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
alembic

# Authentication
python-jose[cryptography]==3.3.0
bcrypt==4.1.2

# Environment
python-dotenv==1.0.0

# PDF Processing
paddleocr
paddlepaddle
pdfplumber      # Enhanced PDF extraction (tables/layout)
PyMuPDF         # Fast PDF processing (fitz)

# Document Processing (Multiple Formats)
python-docx     # Word documents
markdown        # Markdown files
python-pptx     # PowerPoint (optional)
openpyxl        # Excel (optional)
pypandoc        # Universal document converter
PyPDF2

# LLM APIs
openai          # GPT-4, GPT-4o

# NLP and Matching
sentence-transformers
spacy
torch

# Data Processing
pandas
numpy

# Utilities
Pillow                  # Image handling for OCR & preprocessing
python-magic-bin        # For file type detection (Windows)
fuzzywuzzy
python-Levenshtein
pydantic-settings==2.1.0
pydantic[email]
chardet                 # Encoding detection for TXT/MD/RTF

# Optional but recommended for better OCR preprocessing
opencv-python-headless  # Used if available for denoise + adaptive threshold

# Local LLM / extras
ollama
